@using RentalsWebApp.Data.Enums
@using RentalsWebApp.ViewModels
@model UploadStatementViewModel

@{
    ViewData["Title"] = "Upload Stament";
}

<h1>Statement Upload</h1>
<!-- Account page navigation-->
<nav class="nav nav-borders">
    <a class="nav-link" asp-controller="Dashboard" asp-action="EditUserProfile" target="__self">Account Details</a>
    <a class="nav-link" asp-controller="Dashboard" asp-action="Billing" target="__self">Billing</a>
    @if (User.Identity.IsAuthenticated && User.IsInRole("tenant"))
    {
        <a class="nav-link" asp-controller="Dashboard" asp-action="Security" target="__self">Security</a>
    }
    <a class="nav-link" asp-controller="Dashboard" asp-action="Notification" target="__self">Notifications</a>
</nav>
<hr class="mt-0 mb-8">
<div class="col-xl-8">
    <!-- Account details card-->
    <div class="card mb-8">
        <div class="card-header">Statement Upload</div>
        <div class="card-body">
            <form method="post" asp-action="UploadStatement" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" id="UserId" name="UserId" asp-for="UserId" />

               
                <div class="form-group col-md-8">
                    <label asp-for="Month"></label>
                    <select asp-for="Month" class="form-control" asp-items="@Html.GetEnumSelectList<Months>()">
                        <option selected>Select...</option>
                        <option>...</option>
                    </select>
                    <span asp-validation-for="Month" class="text-danger"></span>
                </div>
                <div class="form-group col-md-8">
                    <label asp-for="WaterAmount"></label>
                    <input asp-for="WaterAmount" class="form-control" placeholder="Johannesburg">
                    <span asp-validation-for="WaterAmount" class="text-danger"></span>
                </div>
                <div class="form-group col-md-8">
                    <label asp-for="ElectricityAmount"></label>
                    <input asp-for="ElectricityAmount" class="form-control" placeholder="Johannesburg">
                    <span asp-validation-for="ElectricityAmount" class="text-danger"></span>
                </div>
                <div class="form-group col-md-8">
                    <label asp-for="Statement" class="control-label"></label>
                    <input asp-for="Statement" class="form-control" type="file" placeholder="Electricity and Water bill">
                    <span asp-validation-for="Statement" class="text-danger"></span>
                </div>
                <!-- Form Row-->
                <div>
                    <button type="submit" class="btn btn-sm btn-outline-secondary">Save</button>
                    <a asp-controller="Dashboard" asp-action="Index" class="btn btn-sm btn-outline-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>